# dependencies
/node_modules

_reference-dataset.json

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# NOTE: /public/exercises/*.gif (downloaded exercise animation GIFs) are
# intentionally NOT ignored and ARE committed to this repo, so the deployed
# GitHub Pages site has real images instead of falling back to icons. See
# agent.md for the source license note (hasaneyldrm/exercises-dataset,
# educational/non-commercial use) — this is a personal, non-commercial
# project. Regenerate/re-verify matches with `npm run fetch-photos`.
